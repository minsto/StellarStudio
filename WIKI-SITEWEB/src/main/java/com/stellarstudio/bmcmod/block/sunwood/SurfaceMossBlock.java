package com.stellarstudio.bmcmod.block.sunwood;

import net.minecraft.world.level.block.CarpetBlock;

/** Mousse de surface : texture grise teintée par biome (couleur herbe). */
public final class SurfaceMossBlock extends CarpetBlock {
    public SurfaceMossBlock(Properties properties) {
        super(properties);
    }
}
