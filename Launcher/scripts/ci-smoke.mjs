#!/usr/bin/env node
/**
 * Placeholder smoke test — la CI doit lancer après `npm run build`.
 * Étendre avec Playwright (fenêtre Electron) ou tests IPC en STELLAR_TEST_MODE.
 */
console.log('ci-smoke: OK (build already run). Add e2e / IPC tests when ready.')
process.exit(0)
